# armojidots-hypr

Hyprland desktop environment dotfiles — companion to
[armojidots](https://github.com/Armoji-code/armojidots) (the SwayFX build).
Managed with GNU Stow: each top-level directory is a "package" mirroring
$HOME, symlinked into place with `stow <package>`.
